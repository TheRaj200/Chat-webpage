<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Room</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex gap-4 fixed right-[10%] justify-end">
            <a class="bg-pink-800 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/profile">Profile</a>
            <a class="bg-yellow-700 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/chat">Chat Room</a>
            <a class="bg-green-500 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/global">Posts</a>
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/logout">LogOut</a>
        </div>
        <h3 class="text-3xl md:mt-0 mt-[5vh] pt-4">Chat Room</h3>
        <h5>You Can Chat Here.</h5>
        <form class="fixed bottom-[5%] flex w-full md:p-0 md:bottom-[5%] md:left-[50%]" method="post" action="/chat">
            <textarea placeholder="Typing.." class="p-3 block outline-none w-[60%] md:w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class="w-20 text-sm px-3 py-2 bg-blue-500 block rounded-md" type="submit" value="Send">
        </form>
        <div class="posts">
            <div class="postcontainer mt-4">
                <% chat.forEach(function(chatMessage) { %>
                <div class="post md:w-1/3 w-full p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                    <img class="w-10 h-10 rounded-full" src=<%= chatMessage.user.url %> alt="">
                    <h4 class="text-blue-500 mb-2">@<%= chatMessage.user.username %></h4>
                    <p class="text-sm tracking-tight"><%= chatMessage.chat %></p>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-red-500" href="/chat-delete/<%= chatMessage._id %>">Delete</a>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
